// FOR MONDAY: Inheritance
function makeRectangle(w, h) {
    return { 'width': w, 'height': h };
}

// constructor
function Rectangle(w, h) {
    // this = {}
    this.width = w;
    this.height = h;
    // return this;
}

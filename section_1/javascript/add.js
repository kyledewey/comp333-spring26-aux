// FOR NEXT TIME: continue static vs. dynamic typing,
//                show javap -c result with overloading and
//                how it differs, strong vs. weak typing
function add(x, y) {
    return x + y;
}

function doSomething(x) {
    if (x < 3) {
        return 7;
    } else {
        return "foo";
    }
}

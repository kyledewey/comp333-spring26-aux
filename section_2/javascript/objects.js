// NEXT TIME: inheritance
function add(x, y) {
    temp = x + y;
    return temp;
}

function makeRectangle(w, h) {
    // this = {};
    //return { 'width': w, 'height': h };
}

// constructor
function Rectangle(w, h) {
    // this = {};
    this.width = w;
    this.height = h;
    // return this;
}
